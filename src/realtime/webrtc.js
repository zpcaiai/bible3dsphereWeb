// DEPRECATED — mesh WebRTC voice was superseded by the existing LiveKit SFU voice
// feature (see VoiceRoomPage.jsx + backend/routers/voice.py), which delivers
// Zoom-grade audio and scales past 8 participants without a self-hosted coturn.
// Kept as an empty stub because the sandbox FUSE mount disallows file deletion.
// Safe to delete from a normal checkout.
export {}
