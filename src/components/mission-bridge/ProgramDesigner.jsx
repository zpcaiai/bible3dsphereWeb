import { useMemo, useState } from 'react'
import { publishMissionBridgeProgram, validateMissionBridgeProgram } from '../../missionBridgeApi'
import { t } from '../../i18n/runtime'
import './programDesigner.css'

const initial={id:'',version:'1.0.0',groupType:'',title:'',description:'',durationWeeks:8,cadence:'weekly',sessionMode:'hybrid',consentRequirements:['service_participation'],safeguardingProfile:{noCoercion:true,professionalReferral:true},riskTriggers:['self_harm','violence','abuse','medical_emergency'],referralTypes:['medical','mental_health','legal'],outcomeMetrics:['trusted_relationship','goal_progress'],pathways:[{key:'standard',title:'标准路径',eligibility:{},steps:[{key:'step-1',title:'倾听与共同目标',stepType:'required',required:true,contentRefs:[]}]}]}
export default function ProgramDesigner({token,onPublished}){const[draft,setDraft]=useState(initial);const[message,setMessage]=useState('');const[busy,setBusy]=useState('');const steps=draft.pathways[0].steps;const valid=useMemo(()=>draft.id.length>=3&&draft.title.length>=3&&draft.description.length>=10&&steps.length&&steps.every(s=>s.key.length>=2&&s.title.length>=2),[draft,steps]);const patch=(key,value)=>setDraft(old=>({...old,[key]:value}));const updateStep=(index,key,value)=>setDraft(old=>({...old,pathways:[{...old.pathways[0],steps:old.pathways[0].steps.map((s,i)=>i===index?{...s,[key]:value}:s)}]}));const run=async mode=>{setBusy(mode);setMessage('');try{const result=mode==='validate'?await validateMissionBridgeProgram(token,draft):await publishMissionBridgeProgram(token,draft);setMessage(mode==='validate'?`✓ ${t('验证通过')} · ${result.steps} ${t('个步骤')}`:`✓ ${t('已发布不可变版本')} ${result.version}`);if(mode==='publish')onPublished?.()}catch(err){setMessage(err.message)}finally{setBusy('')}};return <section className="mb-designer"><div className="mb-alert warning">{t('发布后版本不可覆盖。修改项目必须提升版本号；无安全配置的项目不能发布。')}</div><div className="mb-designer-grid"><label>{t('项目 ID')}<input value={draft.id} onChange={e=>patch('id',e.target.value.replace(/[^a-z0-9-]/g,''))}/></label><label>{t('版本')}<input value={draft.version} onChange={e=>patch('version',e.target.value)}/></label><label>{t('群体类型')}<input value={draft.groupType} onChange={e=>patch('groupType',e.target.value)}/></label><label>{t('时长（周）')}<input type="number" min="1" max="104" value={draft.durationWeeks} onChange={e=>patch('durationWeeks',Number(e.target.value))}/></label><label className="wide">{t('项目名称')}<input value={draft.title} onChange={e=>patch('title',e.target.value)}/></label><label className="wide">{t('透明说明')}<textarea value={draft.description} onChange={e=>patch('description',e.target.value)}/></label><label>{t('节奏')}<select value={draft.cadence} onChange={e=>patch('cadence',e.target.value)}><option value="weekly">weekly</option><option value="daily">daily</option></select></label><label>{t('形式')}<select value={draft.sessionMode} onChange={e=>patch('sessionMode',e.target.value)}><option value="hybrid">hybrid</option><option value="online">online</option><option value="offline">offline</option><option value="async">async</option></select></label></div><div className="mb-safety-checks"><label><input type="checkbox" checked={draft.safeguardingProfile.noCoercion} onChange={e=>patch('safeguardingProfile',{...draft.safeguardingProfile,noCoercion:e.target.checked})}/>{t('禁止强迫')}</label><label><input type="checkbox" checked={draft.safeguardingProfile.professionalReferral} onChange={e=>patch('safeguardingProfile',{...draft.safeguardingProfile,professionalReferral:e.target.checked})}/>{t('专业转介')}</label></div><h3>{t('路径步骤')}</h3><div className="mb-step-list">{steps.map((step,index)=><div key={index}><input aria-label={t('步骤键')} value={step.key} onChange={e=>updateStep(index,'key',e.target.value)}/><input aria-label={t('步骤标题')} value={step.title} onChange={e=>updateStep(index,'title',e.target.value)}/><select value={step.stepType} onChange={e=>updateStep(index,'stepType',e.target.value)}><option value="required">required</option><option value="optional">optional</option><option value="conditional">conditional</option></select><button type="button" aria-label={t('删除步骤')} onClick={()=>setDraft(old=>({...old,pathways:[{...old.pathways[0],steps:old.pathways[0].steps.filter((_,i)=>i!==index)}]}))}>×</button></div>)}</div><button type="button" className="secondary" onClick={()=>setDraft(old=>({...old,pathways:[{...old.pathways[0],steps:[...old.pathways[0].steps,{key:`step-${steps.length+1}`,title:'',stepType:'required',required:true,contentRefs:[]}]}]}))}>＋ {t('添加步骤')}</button>{message&&<div className="mb-alert success">{message}</div>}<div className="mb-actions"><button type="button" disabled={!valid||busy} onClick={()=>run('validate')}>{t('发布前验证')}</button><button type="button" disabled={!valid||busy} onClick={()=>run('publish')}>{t('发布版本')}</button></div></section>}
