# Formation Data Lifecycle Policy

Use purpose limitation, shortest useful retention, minor minimization, aggregation and deletion. Users can export, correct, pause, revoke sharing and delete user-owned data. Safety/audit exceptions must be narrow, documented, time-bound where possible and never silently deny the request. Exports exclude other people’s data.
