# Scenario Runtime State Policy

Pin every session to a scenario version. Persist node IDs, choice IDs and state—not raw free text. Support pause/resume/expire and idempotent events. A published scenario update never silently changes an active session. Do not derive personality, risk or spiritual profiles from paths.
