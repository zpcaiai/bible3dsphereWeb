interface:
  display_name: "Corpus, Fixtures and Benchmarks"
  short_description: "Run this Batch capability with evidence gates"
  default_prompt: "Use $b10-behavioral-equivalence-differential-corpus-benchmark to execute this workflow with explicit evidence, validation, human approval boundaries, and rollback reporting."
policy:
  allow_implicit_invocation: true
