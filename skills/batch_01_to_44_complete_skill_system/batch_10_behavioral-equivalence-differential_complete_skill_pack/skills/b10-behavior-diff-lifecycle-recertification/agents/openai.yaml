interface:
  display_name: "Lifecycle and Continuous Recertification"
  short_description: "Run this Batch capability with evidence gates"
  default_prompt: "Use $b10-behavior-diff-lifecycle-recertification to execute this workflow with explicit evidence, validation, human approval boundaries, and rollback reporting."
policy:
  allow_implicit_invocation: true
