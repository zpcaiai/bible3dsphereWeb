interface:
  display_name: "Batch 16: Target Architecture Search and Migration Planning"
  short_description: "Orchestrate this Batch with evidence gates"
  default_prompt: "Use $batch-16-target-architecture-search to execute this workflow with explicit evidence, validation, human approval boundaries, and rollback reporting."
policy:
  allow_implicit_invocation: true
