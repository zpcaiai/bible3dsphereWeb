interface:
  display_name: "b03-ir-query-diff-and-export-service"
  short_description: "Run this Batch capability with evidence gates"
  default_prompt: "Use $b03-ir-query-diff-and-export-service to execute this workflow with explicit evidence, validation, human approval boundaries, and rollback reporting."
policy:
  allow_implicit_invocation: true
