interface:
  display_name: "b03-callgraph-dispatch-reflection-and-dynamic-linker"
  short_description: "Run this Batch capability with evidence gates"
  default_prompt: "Use $b03-callgraph-dispatch-reflection-and-dynamic-linker to execute this workflow with explicit evidence, validation, human approval boundaries, and rollback reporting."
policy:
  allow_implicit_invocation: true
