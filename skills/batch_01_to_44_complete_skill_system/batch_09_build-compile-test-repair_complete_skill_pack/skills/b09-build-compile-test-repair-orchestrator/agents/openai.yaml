interface:
  display_name: "Orchestrator and State Machine"
  short_description: "Run this Batch capability with evidence gates"
  default_prompt: "Use $b09-build-compile-test-repair-orchestrator to execute this workflow with explicit evidence, validation, human approval boundaries, and rollback reporting."
policy:
  allow_implicit_invocation: true
