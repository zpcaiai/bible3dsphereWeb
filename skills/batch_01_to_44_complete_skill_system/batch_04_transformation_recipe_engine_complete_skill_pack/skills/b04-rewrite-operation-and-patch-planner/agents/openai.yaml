interface:
  display_name: "b04-rewrite-operation-and-patch-planner"
  short_description: "Run this Batch capability with evidence gates"
  default_prompt: "Use $b04-rewrite-operation-and-patch-planner to execute this workflow with explicit evidence, validation, human approval boundaries, and rollback reporting."
policy:
  allow_implicit_invocation: true
