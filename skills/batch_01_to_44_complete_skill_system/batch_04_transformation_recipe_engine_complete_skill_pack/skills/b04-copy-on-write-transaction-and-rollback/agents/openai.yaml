interface:
  display_name: "b04-copy-on-write-transaction-and-rollback"
  short_description: "Run this Batch capability with evidence gates"
  default_prompt: "Use $b04-copy-on-write-transaction-and-rollback to execute this workflow with explicit evidence, validation, human approval boundaries, and rollback reporting."
policy:
  allow_implicit_invocation: true
