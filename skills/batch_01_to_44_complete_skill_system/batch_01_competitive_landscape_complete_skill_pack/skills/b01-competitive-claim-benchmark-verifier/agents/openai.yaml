interface:
  display_name: "b01-competitive-claim-benchmark-verifier"
  short_description: "Run this Batch capability with evidence gates"
  default_prompt: "Use $b01-competitive-claim-benchmark-verifier to execute this workflow with explicit evidence, validation, human approval boundaries, and rollback reporting."
policy:
  allow_implicit_invocation: true
