interface:
  display_name: "b01-competitor-discovery-registry"
  short_description: "Run this Batch capability with evidence gates"
  default_prompt: "Use $b01-competitor-discovery-registry to execute this workflow with explicit evidence, validation, human approval boundaries, and rollback reporting."
policy:
  allow_implicit_invocation: true
