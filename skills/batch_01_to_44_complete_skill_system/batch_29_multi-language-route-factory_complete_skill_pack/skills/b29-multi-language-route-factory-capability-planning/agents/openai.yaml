interface:
  display_name: "Capability Matrix and Planning"
  short_description: "Run this Batch capability with evidence gates"
  default_prompt: "Use $b29-multi-language-route-factory-capability-planning to execute this workflow with explicit evidence, validation, human approval boundaries, and rollback reporting."
policy:
  allow_implicit_invocation: true
