interface:
  display_name: "b05-build-dependency-toolchain-generator"
  short_description: "Run this Batch capability with evidence gates"
  default_prompt: "Use $b05-build-dependency-toolchain-generator to execute this workflow with explicit evidence, validation, human approval boundaries, and rollback reporting."
policy:
  allow_implicit_invocation: true
