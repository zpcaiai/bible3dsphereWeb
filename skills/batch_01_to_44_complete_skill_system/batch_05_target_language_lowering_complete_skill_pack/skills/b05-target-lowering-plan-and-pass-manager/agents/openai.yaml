interface:
  display_name: "b05-target-lowering-plan-and-pass-manager"
  short_description: "Run this Batch capability with evidence gates"
  default_prompt: "Use $b05-target-lowering-plan-and-pass-manager to execute this workflow with explicit evidence, validation, human approval boundaries, and rollback reporting."
policy:
  allow_implicit_invocation: true
