interface:
  display_name: "b05-exception-resource-lifetime-lowerer"
  short_description: "Run this Batch capability with evidence gates"
  default_prompt: "Use $b05-exception-resource-lifetime-lowerer to execute this workflow with explicit evidence, validation, human approval boundaries, and rollback reporting."
policy:
  allow_implicit_invocation: true
