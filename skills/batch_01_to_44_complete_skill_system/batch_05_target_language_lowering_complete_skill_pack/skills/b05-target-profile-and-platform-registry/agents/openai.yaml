interface:
  display_name: "b05-target-profile-and-platform-registry"
  short_description: "Run this Batch capability with evidence gates"
  default_prompt: "Use $b05-target-profile-and-platform-registry to execute this workflow with explicit evidence, validation, human approval boundaries, and rollback reporting."
policy:
  allow_implicit_invocation: true
