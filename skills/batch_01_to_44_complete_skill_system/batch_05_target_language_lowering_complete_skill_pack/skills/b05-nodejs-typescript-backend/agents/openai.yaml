interface:
  display_name: "b05-nodejs-typescript-backend"
  short_description: "Run this Batch capability with evidence gates"
  default_prompt: "Use $b05-nodejs-typescript-backend to execute this workflow with explicit evidence, validation, human approval boundaries, and rollback reporting."
policy:
  allow_implicit_invocation: true
