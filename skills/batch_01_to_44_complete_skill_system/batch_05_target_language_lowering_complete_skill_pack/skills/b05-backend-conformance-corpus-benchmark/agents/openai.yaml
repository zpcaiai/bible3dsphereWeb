interface:
  display_name: "b05-backend-conformance-corpus-benchmark"
  short_description: "Run this Batch capability with evidence gates"
  default_prompt: "Use $b05-backend-conformance-corpus-benchmark to execute this workflow with explicit evidence, validation, human approval boundaries, and rollback reporting."
policy:
  allow_implicit_invocation: true
