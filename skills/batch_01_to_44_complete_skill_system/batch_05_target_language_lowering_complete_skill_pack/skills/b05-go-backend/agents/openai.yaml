interface:
  display_name: "b05-go-backend"
  short_description: "Run this Batch capability with evidence gates"
  default_prompt: "Use $b05-go-backend to execute this workflow with explicit evidence, validation, human approval boundaries, and rollback reporting."
policy:
  allow_implicit_invocation: true
