interface:
  display_name: "Discovery and Inventory"
  short_description: "Run this Batch capability with evidence gates"
  default_prompt: "Use $b28-relational-database-12-routes-discovery-inventory to execute this workflow with explicit evidence, validation, human approval boundaries, and rollback reporting."
policy:
  allow_implicit_invocation: true
