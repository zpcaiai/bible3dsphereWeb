interface:
  display_name: "Batch 2：应用现代化自动评估"
  short_description: "Orchestrate this Batch with evidence gates"
  default_prompt: "Use $batch-02-application-modernization-automated-assessment to execute this workflow with explicit evidence, validation, human approval boundaries, and rollback reporting."
policy:
  allow_implicit_invocation: true
