interface:
  display_name: "Conservative Certification Gate"
  short_description: "Run this Batch capability with evidence gates"
  default_prompt: "Use $b21-real-repository-golden-workload-certification-gate to execute this workflow with explicit evidence, validation, human approval boundaries, and rollback reporting."
policy:
  allow_implicit_invocation: true
