interface:
  display_name: "Batch 21: Real Repository Slice and Golden Workload Factory"
  short_description: "Orchestrate this Batch with evidence gates"
  default_prompt: "Use $batch-21-real-repository-golden-workload to execute this workflow with explicit evidence, validation, human approval boundaries, and rollback reporting."
policy:
  allow_implicit_invocation: true
