interface:
  display_name: "Deterministic Engine"
  short_description: "Run this Batch capability with evidence gates"
  default_prompt: "Use $b14-formal-verification-proof-carrying-deterministic-engine to execute this workflow with explicit evidence, validation, human approval boundaries, and rollback reporting."
policy:
  allow_implicit_invocation: true
