interface:
  display_name: "Human Review and Approval"
  short_description: "Run this Batch capability with evidence gates"
  default_prompt: "Use $b13-evidence-graph-continuous-certification-human-approval to execute this workflow with explicit evidence, validation, human approval boundaries, and rollback reporting."
policy:
  allow_implicit_invocation: true
