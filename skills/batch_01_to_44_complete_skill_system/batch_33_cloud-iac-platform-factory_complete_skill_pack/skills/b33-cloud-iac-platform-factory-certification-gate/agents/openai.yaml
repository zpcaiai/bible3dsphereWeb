interface:
  display_name: "Conservative Certification Gate"
  short_description: "Run this Batch capability with evidence gates"
  default_prompt: "Use $b33-cloud-iac-platform-factory-certification-gate to execute this workflow with explicit evidence, validation, human approval boundaries, and rollback reporting."
policy:
  allow_implicit_invocation: true
