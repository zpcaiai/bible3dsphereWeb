interface:
  display_name: "Adapter and Provider Layer"
  short_description: "Run this Batch capability with evidence gates"
  default_prompt: "Use $b34-portfolio-scale-migration-factory-adapter-provider to execute this workflow with explicit evidence, validation, human approval boundaries, and rollback reporting."
policy:
  allow_implicit_invocation: true
