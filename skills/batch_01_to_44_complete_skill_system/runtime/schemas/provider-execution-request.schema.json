{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://elmos.local/schemas/provider-execution-request.json",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "skill", "adapter_id", "operation", "parameters", "idempotency_key", "fencing_token", "source_fingerprint", "approval", "compensates_request_sha256"],
  "properties": {
    "schema_version": {"const": "1.0"},
    "skill": {"type": "string", "minLength": 1, "maxLength": 64},
    "adapter_id": {"type": "string", "pattern": "^[a-z][a-z0-9-]{0,62}[a-z0-9]$"},
    "operation": {"type": "string", "pattern": "^[a-z][a-z0-9-]{0,62}[a-z0-9]$"},
    "parameters": {"type": "object"},
    "idempotency_key": {"type": "string", "pattern": "^[a-z][a-z0-9-]{0,62}[a-z0-9]$"},
    "fencing_token": {"type": "integer", "minimum": 1},
    "source_fingerprint": {"type": "string", "pattern": "^sha256:[0-9a-f]{64}$"},
    "approval": {"type": ["object", "null"]},
    "compensates_request_sha256": {"type": ["string", "null"], "pattern": "^sha256:[0-9a-f]{64}$"}
  }
}
