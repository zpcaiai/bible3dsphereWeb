interface:
  display_name: "Batch 20: Skill SDK, Runtime, Registry and Marketplace Productization"
  short_description: "Orchestrate this Batch with evidence gates"
  default_prompt: "Use $batch-20-skill-sdk-runtime-registry to execute this workflow with explicit evidence, validation, human approval boundaries, and rollback reporting."
policy:
  allow_implicit_invocation: true
