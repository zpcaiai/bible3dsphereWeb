interface:
  display_name: "Failure Recovery and Rollback"
  short_description: "Run this Batch capability with evidence gates"
  default_prompt: "Use $b43-legacy-coexistence-retirement-failure-recovery to execute this workflow with explicit evidence, validation, human approval boundaries, and rollback reporting."
policy:
  allow_implicit_invocation: true
