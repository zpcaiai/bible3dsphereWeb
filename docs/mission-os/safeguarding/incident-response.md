# Incident Response

L0 follows ordinary workflow. L1 receives trained support. L2 creates urgent human safeguarding and professional review. L3 bypasses normal queues for immediate local emergency and incident-command response. Risk downgrade is human-only with a reason; L3 closure requires an incident commander and two independent approvals, followed by review.
